import { useState } from 'react';
import Component from 'module-template';

function App() {
  const [count, setCount] = useState(0);

  return (
    <div className="App">
      <Component />
    </div>
  );
}

export default App;
